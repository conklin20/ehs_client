import {  } from './notificationMessages';

//parses the error from the api and returns a user friendly message to display 
export default function errorMessageFormatter(res) {
    console.log(res); 
}